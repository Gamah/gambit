# M14 — Attempt 4 plan: seated terries, arms on the table, hand follows the piece

> Attempts 1–3 were abandoned; their distilled history is preserved at the bottom of this file
> (read it — every trap there was paid for). This top half is the **live plan** for attempt 4,
> built on a spec the owner has since **simplified**: the reach requirement is waived.

## The spec (owner intent, 2026-07-19 — supersedes the old "goal" where they conflict)

1. **Terry faces the board** (the confirmed M13 facing bug is priority #1 — it ate Attempt 3).
2. **Idle pose**: while not moving (opponent's turn or thinking), terry sits with arms resting
   on the table. Anchor positions are tunable offsets — "whatever reads best".
3. **On a committed move**: the right hand snaps to a spot over the moved piece, follows the
   piece as it slides, then eases back to the idle pose.
4. **Parallel hands**: if the opponent moves while a gesture is still playing, both hands
   animate independently — no abort/retract logic (owner's explicit choice).
5. **Cadence-slewed lerp times** like lichess web: a deliberate default (owner: 0.2s is too
   quick as a default), shortening toward lichess-quick when moves come rapidly. Separate
   own/opponent base durations, tunable. All knobs global across every terry.
6. **REACH IS WAIVED**: no half-rise, no stand. The seated arm may lean/clip **through** the
   board to reach far squares. Stand-to-reach is an explicitly abandoned stretch goal.
7. **One branch, everything together** — but the `gambit_terry` probe still lands as commit 1
   (the lesson from three attempts: measure, never guess).

Still pinned from the original design: move-only hand (no hover tracking), everyone sees the
hand including the mover, captures are minimal in v1 (victim slides to its tray alone, no hand).

Branch: `m14-attempt-4`. Kill chain: `ChessRing.TerrySeated` (M13, exists) → new
`ChessRing.TerryHands` sub-switch; both off = the shipped M13 world. The facing fix and seat
setback are **M13 bug fixes** and live under `TerrySeated` only, NOT `TerryHands`.

## Verified current state (2026-07-19, file:line)

- **Facing bug confirmed on master**: `BeginEngage` disables the controller
  (`LobbyPlayer.cs:746`), silencing `OnRotateRenderBody`; `PlantOnSeat` rotates only the ROOT
  (`:1128`). The Body child (SkinnedModelRenderer GO) keeps its walk-up heading; nothing ever
  writes it. `ApplySitPose` (`:946-975`) already runs EVERY frame for local (`:255-259`) and
  proxies (`:370-388`) — the natural home for the fix.
- `SeatedAt` (`:314`) — `(ChessStation, ChessSeat)?` derived per-frame from `[Sync]` occupancy
  on every client; `_switching` (`:1059`) marks the seat-switch schwoop. Both verified present.
- `ChessRing.SeatSitBack = 26f` (`ChessRing.cs:116`) — the un-reverted Attempt-1 scoot; chest
  buried in the table. 36 is the working centre. `ChairCenterX => SeatSitBack +
  ChairSeatOffsetX` (`:1354`) — the chair follows the fix, which is the invariant we want.
- **Piece animation already exists**: `ChessBoardView.SyncPieces` (`:252-354`) FEN-diffs per
  frame, pairs vanished→appeared squares by FEN char, creates `Slide{Seconds=MoveSeconds
  (0.22f), Arc}` per mover; `AdvanceSlides` (`:462-492`) ease-out-cubic + sine arc. Captures
  adopt the victim GO into a tray slide (`CaptureSeconds=0.45f`). Multi-ply lichess bursts
  collapse to one net diff. Local/lichess/remote all render through the identical path.
- Watcher pattern: `TableSounds` + `BoardDiff.Between(fen,ply → Move/Rewind/None, whiteMoved,
  capture)` (`Chess/BoardDiff.cs:43-68`). But the hand needs the live sliding **GameObject**,
  which only the view has → hands integrate with `ChessBoardView` (itself on the `IBoardGame`
  seam, so lichess/local/any future source gets hands by existing — this is NOT the
  call-from-a-controller mistake).
- NO SetIk/bone-override/hand code survives on master; `gambit_terry` does not exist (prose
  only — its old readings are baked into the chair constants).

## Implementation (ordered commits, one branch)

### Commit 1 — `gambit_terry` probe (no behavior change)

**New `client/Code/World/TerryDiagnostics.cs`**, `[ConCmd("gambit_terry")]` (pattern:
`PerftCommand.cs`). Per seated `LobbyPlayer`: station/seat/IsProxy, **want yaw** (the
deterministic derivation below), **root yaw**, **body yaw** (Body child GO), **station yaw**,
root-vs-`SeatSitWorldPosition` delta, knob dump (SeatSitBack/SitZ/SitOffsetHeight/
ChairSeatTopZ). Add `public SkinnedModelRenderer BodyRenderer => _bodyRenderer;` to
`LobbyPlayer` (also used by TerryHands). Later commits extend the dump with hand state.

### Commit 2 — facing fix + setback

**Facing** — in `ApplySitPose` (runs every frame, local + proxy, controller silenced on both
paths so nothing fights the write): re-assert the Body child's `WorldRotation` from the
**station transform only**. Attempt-3's desync came from locally-derived rotation; this math
is identical on every machine — the same LookAt as `PlantOnSeat`:

```csharp
if ( SeatedAt is { } at && _bodyRenderer != null )
{
    var seatPos = at.Station.SeatSitWorldPosition( at.Seat );
    var flat = at.Station.WorldPosition; flat.z = seatPos.z;
    var toBoard = flat - seatPos;
    if ( toBoard.Length > 0.01f )
        _bodyRenderer.GameObject.WorldRotation = _switching
            ? WorldRotation                          // ride the seat-switch schwoop
            : Rotation.LookAt( toBoard, Vector3.Up );
}
```

Rotation only — never the child's position. Stand-up needs no undo (the controller resumes
locally; the proxy path restores `UseAnimatorControls`).

**Setback** — `SeatSitBack` 26f → **36f**; rewrite its doc comment (the "now moot but
harmless" claim is the wrong one this file corrected). `ChessStation`'s `?? 36f` fallback now
matches. The chair moves back with it via the `ChairCenterX` derivation — intended.

### Commit 3 — Sandbox-free core in `Code/Chess/` (harness-proven, no Sandbox deps)

- **`HandGesture.cs`** — per-seat gesture clock: `Phase { Rest, Snap, Follow, Return }`,
  `Begin(snapSeconds, followSeconds, returnSeconds)` (restart-safe: a mid-gesture Begin
  captures the current weight, no discontinuity), `CancelToRest()`, `Advance(dt)`, `Weight`
  (0=rest anchor, 1=piece; eased). One-clock lesson kept: the engine side computes
  `target = lerp(restWorld, pieceGO.WorldPosition + grip, Weight)` from the **live piece GO**
  every frame — the piece slide stays authoritative, the hand is derived and can only ever be
  a little off the *right* answer.
- **`MoveCadence.cs`** — the lichess-style slew: `RecordMove(now)` keeps an EMA of the
  inter-move gap (α≈0.5, so one slow move after a blitz burst doesn't yank durations back up
  instantly); `Scale01(fastGap, slowGap)` smoothstepped;
  `duration = quick + (base − quick) · scale`. `Reset()` on rewind/source swap.
- **`HandPose.cs`** — `AimAngles(dx,dy,dz) → (pitch, yaw)` of the shoulder→target bearing.
  Fixes Attempt-2's kinked-wrist jank: pitch derives from the bearing, never a fixed value.
  Roll stays a knob (it drives the elbow pole; roll 0 = t-rex arm).
- **Harness** (scratch csproj, `net10.0` + `ImplicitUsings`, per CLAUDE.md's recipe): timeline
  phase order + weight continuity + mid-gesture restart + two independent interleaved
  instances (the parallel-hands case); cadence endpoints/monotonicity/EMA/reset; aim-angle
  quadrant checks; BoardDiff regression rides along free.

### Commit 4 — TerryHands + knobs + view integration

**`ChessRing` knobs** (all `[Property]`, code defaults on the runtime-built ring — global
one-slider-moves-all by the existing `ChessRing.Instance` pattern):
`TerryHands = true`; rest anchors `HandRestX ≈ 25` (between board edge ~19.5 and table edge,
base units × TableScale), `HandRestY ≈ 6` (mirrored per hand; +Y is LEFT), `HandRestLift ≈
0.5`; `GripHeight ≈ 4`; `HandRollDeg ≈ 35`; `ShoulderHeight ≈ 52`; `SnapSeconds = 0.08`,
`ReturnSeconds = 0.35`; `MoveSecondsOwn = 0.40`, `MoveSecondsOpp = 0.45`,
`MoveSecondsQuick = 0.18`, `CadenceFastGap = 1.0`, `CadenceSlowGap = 5.0`.
`BuildStation` adds `TerryHands` beside `TableSounds`/`ChessBoardView`, wires `view.Hands`.
**No holdtype forcing** (Attempt-3's invisible-object grip bug); v1 has no finger pose at all.

**New `client/Code/World/TerryHands.cs`** — per-station, two per-seat slots (parallel hands
for free — each seat its own `HandGesture`). API: `BeginGesture(whiteMoved, targetPieceGO,
followSeconds, cadenceScale)`, `CancelGestures()`. `OnUpdate` per seat:
1. Gate on `ChessRing.Instance is { TerrySeated: true, TerryHands: true }` else release IK
   once and return.
2. Resolve the seated player via `Scene.GetAllComponents<LobbyPlayer>()` → `SeatedAt` match
   (cached, revalidated); no player/renderer → release.
3. Rest anchors world-derived from station transform + knobs.
4. Left hand: `SetIk("hand_left", restTransform)` always while seated.
5. Right hand: advance gesture; `pieceWorld = target.IsValid() ? target.WorldPosition +
   up·GripHeight : restWorld` (a destroyed piece — promotion/resync — degrades to rest);
   `pos = lerp(rest, piece, Weight)`; rotation from `HandPose.AimAngles(target −
   approxShoulder)` + `±HandRollDeg`; `SetIk("hand_right", …)` — **WORLD transform** (a local
   one parks the hand at the floor).
6. Seat emptied / stood up → release IK.

Arm ≈ 20u vs 34u board: far squares straighten the arm and the hand stops short / leans
through — explicitly acceptable now. No bone overrides, no servo, no pre-compensation.

**`ChessBoardView`** changes:
- Add `_lastDiffFen/_lastPly`; classify each FEN change with `BoardDiff.Between` (first
  sync/join → `None` → no gesture on join or initial fill).
- On `Move`: `_cadence.RecordMove`; compute one slewed `moveSeconds` for every slide in this
  diff (base = Own if the viewer is the mover seated here, else Opp); scale `CaptureSeconds`
  by the same factor so tray trips keep pace in blitz. On `Rewind`: `_cadence.Reset()`,
  `Hands?.CancelGestures()`.
- Collect gesture candidates during the pairing pass (created slides) AND leftover-additions
  (covers **promotion**: pawn vanishes, new piece appears with no paired slide — the hand
  hovers the appearing piece). Pick ONE target whose char case matches `whiteMoved`,
  **preferring the king** (castling follows the king), then
  `Hands?.BeginGesture(whiteMoved, target, moveSeconds, scale)`.
- Tray slides are never candidates → captures get no hand (v1 minimal). `TableSounds`
  untouched (its own watcher keeps its own baseline).

**Probe extension**: per-seat gesture phase/weight, target piece + pos, rest anchors,
last-written IK transforms, full hand-knob dump — the "which subsystem dominates the motion"
instrument (two editor sessions were lost across the attempts to sliders that were live but
dominated by something else).

### Commit 5 — docs

Update CLAUDE.md's seated-bodies bullet (the reaching-hands-CUT text, the SeatSitBack note,
the new TerryHands gate + this file as the pointer). This file gets attempt 4's outcome only
AFTER the owner judges it in the editor.

## Verification

**On this host**: dotnet harness green for HandGesture/MoveCadence/HandPose + BoardDiff
regression; careful grep/review for the engine half; push for the owner. Everything visual is
a guess until the owner looks — the checklist below is theirs.

**Owner's editor checklist**, in order:
1. **Facing**: seated terry faces the board from any approach angle, host AND joined client,
   both seats, and after a seat-switch schwoop; `gambit_terry` want/root/body yaws agree
   before trusting eyes. Confirm the Body-child rotation write holds on proxies (the Body
   child is a snapshot GO — join-time state, not continuous sync, so it *should* hold; this is
   exactly the class of claim to measure, and the probe's body-yaw line is the measurement).
2. **Setback 36**: chest clear of the table, hips on the pad, chair/feet placement after the
   ChairCenterX shift.
3. **SetIk ground truth**: chain names `hand_left`/`hand_right`, the world-transform
   requirement, release behavior (pop vs blend), residual warp magnitude at rest.
4. **Rest pose reads**: anchors on-table/off-board, elbows plausible at setback 36, roll knob
   actually swings the elbow, left hand doesn't twist the torso during a right-hand gesture.
5. **Gesture look**: snap speed, grip height, return ease, far-square lean-through, the
   parallel both-hands moment, promotion hover, castling-follows-king.
6. **Cadence feel** in real blitz vs slow play; the Own/Opp base split.
7. **Kill switches**: `TerryHands=false` → M13 bodies with no IK ever written;
   `TerrySeated=false` → full pre-M13 revert.

---

# History — attempts 1–3 (read as HISTORY, not instructions)

Three attempts, three abandonments. Nobody who wrote these notes ever *saw* the thing they
described — the dev host cannot render — so every engineering claim is a **lead to re-verify
in the editor**, attributed to the attempt that made it. At least one confident "fact" here
already proved wrong once (the seat-setback value).

## Attempt 1 — the reaching hand *(branch deleted)*

Set out to IK the seated hand onto the piece; abandoned on the reach proof: **working arm ≈
20u** (`arm_upper_R → arm_lower_R1 → hand_R1`, two-bone IK, no stretch/soften anywhere on the
citizen animgraph's arm path), board 34u deep, seated shoulder ~15u back of the near edge —
honest ceiling ≈ rank 2. A seated idle's only lean is a ~5° spine clamp ≈ 2u. **Trap it
documented**: a "reach clamp" (pull far targets onto a reach sphere) made a diagnostic grid
read all-reachable *because it measured reach to the clamped target* — the hand parked at
~rank 2 for every far piece and never moved. A frozen thing that *looks* satisfied.

Bone names it left (re-verify): eyes are `eye_L`/`eye_R`; spine `pelvis, spine_0..2, neck_0,
head`; sit poses a `sit` enum, `ChairForward = sitting_02` (whether `sit=2` leans the torso
was never checked in the editor).

## Attempt 2 — the IK half-rise *(mechanically WORKED; abandoned for the look)*

Half-rise via pelvis bone override, feet planted by engine foot IK, off-hand bracing, piece
riding the rendered `hand_R` bone. **Reach, pick-up, the spectator mirror and inspector
tuning were all verified across two clients** — the owner's verdict was still "looks like a
COMPLETELY BUSTED GAME". If a half-rise (the stand-to-reach stretch goal) is ever attempted
again, the hard networked mechanics were solved here; the open problem was always the look.
Its leads (re-verify):

- The animgraph solves IK **before** overrides apply → aim each IK target at (true target −
  the override translation its chain rides). Translation-only, a vector subtraction.
- A residual **~5u native warp** survived every override (procedural/twist bones); the answer
  was a closed-loop servo — measure last frame's rendered hand vs the ask, integrate, clamp,
  decay.
- Harness reach 64/64 with the live-measured skeleton (pelvis z ≈ 16.6, not the guessed 23),
  `MaxLean 12`, `RiseLift 0.3`, `SeatSitBack 36`, CHOSEN foot plants (never the animated
  ankles — the sit pose tucks them ~25u back). The rise must be mostly horizontal; the foot
  step is exact leg-triangle arithmetic; the rise search is a descending scan (feasibility is
  non-monotone in the corners).
- **Owner's ranked jank list at the pause**: (1) bent elbow — reach margin cranked to 8 pulled
  every ask inside the reach sphere so the arm never extends (retest that slider first);
  (2) kinked wrist — a *fixed* 60° hand pitch; must derive from the forearm's bearing (attempt
  4 does, via `HandPose`); (3) tempo.
- Torso yaw shipped as the one authored rotation (spine override, ~30° cap) — two-bone IK
  can't turn the chest. Never extend that trick to torso *pitch* (moves head/camera mass).
- Finger leads unfinished: `FingerAdjustment_{L|R}{1-5}_{Bend|Curl|Roll|Spread}` (−60..60) are
  documented on the *first-person arms* graph, **unverified on the third-person citizen**;
  fallback is per-phalanx overrides à la `VrHand` (override rotations don't carry children).
- Unbuilt directions: authored clips via `DirectPlayback.Play(name, target, heading,
  interpTime)`; and the fuller capture choreography (attacker picked up first, both pieces
  share the square, one continuous DropAndSwap beat to the tray, grip morphing) — a *someday*
  idea, not v1.

## Attempt 3 — the one-clock model *(never got past the chair)*

Built the one-clock hand model (piece authoritative, hand derived — attempt 4 keeps this) and
proved it green in a harness, but **never once evaluated it in the editor**: the whole session
was eaten by seated-body problems invisible on this host.

- **The facing bug** (now fixed in commit 2 above) was isolated by a `gambit_terry`
  want/root/body/station probe — `root` was right, `body` ~55° off. The costliest lesson:
  don't guess rotations on a host that can't render. Attempt 3 shipped a 180° flip, then a
  degrees lever, then a per-frame reapply that derived a networked-visible rotation *locally*
  on every machine and desynced host vs client (one 90° off), before the probe found the
  truth. **Measure first, guess never** — and derive anything per-frame from state that is
  identical on every machine.
- **The seat setback**: master ships `SeatSitBack = 26` (the un-reverted Attempt-1 scoot;
  chest ~⅓ into the tabletop). An earlier version of this doc confidently claimed master was
  36 — it was flat wrong. 36 is the working centre; attempt 4 changes the code, not just the
  doc.
- **Two trivial-but-invisible placement bugs**: the rest anchor was authored *on* the board
  (x −16 vs near edge ≈ −19.5); `holdtype=holditem` forced every frame at rest made the idle
  hand grip an invisible object. Obvious in a screenshot, unprovable here.

**The meta-lesson, three times running: the killer is never the hand math — it is seated-body
/ room state invisible on this host.** Hence commit order: probe first, facing+setback second,
hands only after.

## Engine observations (re-verify before trusting)

1. Only four game-facing IK chains: `hand_left`, `hand_right`, `foot_left`, `foot_right` —
   two-bone solves to wrist/ankle via `SetIk(name, worldTransform)`. No finger IK anywhere.
2. The hand IK consumes the target ROTATION and it drives the ELBOW pole: roll the target to
   swing the elbow out (roll 0 = t-rex arm); derive pitch from the forearm's bearing, never
   fixed (kinked wrist).
3. Override TRANSLATIONS carry the whole subtree exactly; override ROTATIONS do NOT carry
   children (~15.8u asked of a rotation, ~3u materialised).
4. The animgraph solves IK BEFORE bone overrides apply; a residual ~5u native warp remains.
   (Attempt 4 uses no overrides, so only the warp note applies — acceptable at GripHeight.)
5. `SetIk` wants a WORLD transform — hand it a local one and the hand parks near the floor.
6. The citizen `SkinnedModelRenderer` must live on a `Body` CHILD GO, never the controller's
   own GO. That child's yaw is controller-driven and freezes on sit — the facing bug's source.
7. Tuning must be GLOBAL across every terry: knobs on the ring, every driver reads the same
   instance. Build the live-values console dump on day one.
8. The dev host cannot render or run engine code. Real math goes in `Code/Chess/` with no
   engine dependency so a dotnet harness can prove it (doctrine: `BoardDiff`,
   `CapturedMaterial`, `MoveSpeech`).

## Where the abandoned code is

All three attempts are in git history; **this file is the distillation and you should not
need to go spelunking**. If you must: Attempt 3's one-clock `MoveGesture`/`HandReach`
(harness-green, never seen in the editor) and Attempt 2's working half-rise + servo +
spectator mirror are in the repository history. Prefer the notes above — they are why each
was abandoned.

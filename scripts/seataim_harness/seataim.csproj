<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>disable</Nullable>
    <AssemblyName>seataim</AssemblyName>
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
  </PropertyGroup>
  <ItemGroup>
    <Compile Include="Shim.cs" />
    <Compile Include="PlayerDataShim.cs" />
    <Compile Include="Tests.cs" />
    <!-- The REAL file, compiled verbatim — the whole point. Relative so this runs from
         any clone: `dotnet run` from this folder. -->
    <Compile Include="../../client/Code/World/SeatAim.cs" />
  </ItemGroup>
</Project>

# PLAN.md — Terry's Gambit: what's left

How the game is built and the s&box lore live in **`CLAUDE.md`**. The gamchess API
contract lives in **`README.md`**. This file is only ever upcoming work.

**This is a list of things that need doing, ranked 1–100, highest first. Nothing else.**

- **The number is a priority, not a schedule and not an order.** It says what would hurt most
  to leave undone, not what to do next. Nothing here blocks on the item above it unless the row
  says so.
- **Rows are not branches.** Group them into whatever branches make sense when you pick them
  up — several small rows on one wall or one panel are usually one branch, and one big row
  (chat, voice) is usually its own. The table is deliberately flat so it can be regrouped
  freely; do not read the ranking as a milestone plan.
- **Delete a row when it ships.** The reasoning that outlives the work belongs in CLAUDE.md;
  everything else belongs in git. If a row is only *recorded* — a decision not to do something —
  it says so, and it stays.
- Rows marked **room** need someone standing in the game. Nothing on this host can render or
  make a sound, so those cannot be closed by review.

**Right now this file is two efforts: fix chat (delete ours, use the engine's) and copy voice
from `../terryball`.** Everything that shipped or was set aside is in git history, not here.

| Pri | Item | What needs doing, and the trap in it |
|---|---|---|
| 80 | Chat: delete ours, use the engine's | `ChatShowUI` is **false** and `ChatPanel.razor` is **288 lines** — the engine overlay is off purely so we can redraw it worse. Reference is **`../terryball`** (`8ad9f4b`, *"Drop the rotaliate-copied custom chat box; use the built-in chat"*), **not** `../rotaliate-client`; Gambit is running the code terryball threw away. Deliverable ≈73 lines of hint + the flag: flip `ChatShowUI`, gut the feed/TextEntry/fade, keycap from `Input.GetButtonOrigin( "Chat" )` (never hardcoded), keep `ChatPanel.IsOpen` as a stub returning false so `LobbyPlayer.cs:219`'s gate compiles — that gate becomes dead and that is fine. `Chat` is already bound to **T** and the overlay reads it. |
| 70 | Two lies in `ChatPanel.razor` | `:16` says the key is "rebindable in Settings" — it isn't, and **nothing anywhere writes `PlayerData.Bindings`** (ChatPanel is its only reader, so the whole PlayerData branch is dead code guarding a feature that does not exist — `GamepadBindings` is real, don't confuse them). The header comment describes the feed the row above deletes. |
| 40 | Proximity voice, copied from `../terryball` | A chess lobby is a social room. **terryball has a complete working implementation and it is the reference** (rotaliate-client has no voice at all). **It is small**: `TerryVoice.cs` 23 lines, `VoicePrefs.cs` 61, `VoiceScreen.cs` 222, `VoicePanel.razor` 246, plus 8 lines of host-side spawn. **Game code networks nothing** — playback gates on the **receiver** in `ShouldHearVoice( Connection c ) => VoicePrefs.VoiceEnabled && !VoicePrefs.IsMuted( c.SteamId )`, which runs on the receiver carrying the *sender's* connection, so mute needs no sync, no authority, no server state. Transmit gates owner-locally via `Voice.Mode`; **never touch a networked Enabled flag.** Fits Gambit: `LobbyNetworkManager` already clones + `NetworkSpawn`s `PlayerTemplate` per connection — same seam as terryball's `SpawnAvatar`. |
| 40 | Voice: the open taste call | **Proximity-only, or also table-scoped?** Two players seated opposite are ~50 units apart in an 800-unit room — proximity may already be exactly right, or may make the whole ring one conversation. A loud call, same family as the M11 sound decisions. **Resolve before starting.** |
| 38 | Voice: the gotchas that are the reason to copy | **`Voice.OnUpdate` is sealed** — only the hear/exclude hooks are virtual; don't design around intercepting the update. **The default `Falloff` is a trap** — savagely front-loaded, ~4% volume by 20% of range, which reads as broken voice rather than distant voice; terryball uses a linear `Curve`, `Volume = 2f`. **The engine's default `Distance` of 15,000u is wrong for any room-sized scene** — Gambit's room is 800u, so this needs its own number, **not terryball's 3000**. **`V` is the engine's push-to-talk**, so it can't be the toggle (terryball uses `G`; check Gambit's `Input.config` — `use` is E, `view` is C). A **never-bound sentinel action** is how you hard-gate transmit closed. **The engine has the last word on the mic** — `Voice.IsListening` honours the user's `voip_mode`, which game code cannot change, so a UI must not claim to be the switch. **Menu keys leak into the world** — Enter opens the built-in chat (`Input.Clear( "chat" )`), Space jumps (`UseInputControls = false` gates movement only); track the restore with its own flag and undo it in `OnDisabled`/`OnDestroy`. `WorldspacePlayback = true` makes it positional; `Renderer = <citizen renderer>` gets lip-sync free. Speaking indicator is `voice.LastPlayed < 0.3f`. **Master default is OFF.** |
| 38 | Voice: what to strip, and terryball's stale prose | **Don't copy terryball's comments blindly** — `VoiceScreen.cs:10`, `VoicePanel.razor:17` and `LocalHud.cs:59` all say "V toggles voice" while the code binds **G**; `AlleyServer.cs:93` sets `Distance = 3000f` while its commit claims 4000u. Read the code, not the prose. **Strip**: the first-run help pop-up (`RequestOpenHelp`/`HelpOpen`, unrelated to voice), `LocalIsBowling()` (G is overloaded as the bowler's exit key), `TerryAvatar`/`ControlsOpen` → Gambit's `LobbyPlayer`. `INetworkListener` **fires on every component that implements it** — with N lanes terryball got N avatars per joiner; Gambit has one `LobbyNetworkManager`, so that is a trap to avoid re-introducing, not one to fix. **The HUD-parenting rule**: a client-local HUD must not hang off a networked object (the host's mute/panel state rides the snapshot onto every client), but a hand-placed `NetworkMode.Never` object never reaches joiners either (a joiner rebuilds from the snapshot, which **excludes `Never` objects**) — terryball uses a `GameObjectSystem`. Gambit's screens self-attach at runtime and sidestep this, but **anything voice-related parented into the scene needs this thought through.** |

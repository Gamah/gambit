# Terry's Gambit — chess/lichess fork of rotaliate-client

Published title: **Terry's Gambit**. Internal/repo/package name: `gambit` (s&box `Ident: gambit`, `Org: gamah`; code namespace `Gambit.*`).

## Context

Fork `/home/ai/rotaliate-client` (s&box lobby game, org `gamah`) into `/home/ai/gambit`: keep the social walk-around lobby with stations in a ring, but each station becomes a **chess board** instead of an arcade cabinet, and the custom Go backend is replaced by **lichess**. Logged-in players (lichess OAuth) play real lichess games against anyone on lichess from inside the game; anonymous players play locally at a shared board and get a shareable lichess link afterward via PGN import. The checkerboard floor stays, but its color pops become flat 2D chess-piece glyphs on both square colors. All art must be CC0.

**No feature implementation this session.** On approval, this session does exactly:
1. Copy `/home/ai/rotaliate-client` → `/home/ai/gambit` (excluding `.git/`, Rotaliate thumbnails).
2. Mechanical renames: `rotaliate.sbproj` → `gambit.sbproj` (`Title: Gambit`, `Ident: gambit`, keep `Org: gamah`), `rotaliate/` dir → `gambit/`, `.slnx`/`.csproj` renames, namespace `Rotaliate.*` → `Gambit.*` repo-wide (incl. `.razor` `@namespace` and log prefixes), lobby name → "Gambit v0.1 alpha", set `HttpAllowList: ["https://lichess.org/"]`, rewrite README/CLAUDE.md.
3. Write this plan (sections below) as `PLAN.md` at the repo root.
4. `git init`, commit (pristine copy first, then renames — no AI attribution in commits), create **private** GitHub repo `Gamah/gambit`, push.

Everything from "Milestones" onward is future work executed on later sessions; each milestone gate is tested by the user in the s&box editor (this host has no toolchain — verification is by code review + user testing).

## Research summary

### Lichess API facts

- **Auth**: OAuth2 Authorization Code + **PKCE only** (S256). No client registration or secret — `client_id` is an arbitrary string (use `gambit.gamah`). Desktop pattern: system browser → `http://localhost:{port}` loopback redirect → `POST /api/token`. Tokens live ~1 year, **no refresh tokens** (on 401/expiry: clear + re-auth); `DELETE /api/token` = logout; `POST /api/token/test` = validate. Personal `lip_` tokens (created at lichess.org/account/oauth/token) work as `Authorization: Bearer` — fine for dev, and **token-paste is the guaranteed-shippable fallback** if s&box's sandbox can't open a browser/bind a loopback listener.
- **Scopes**: `board:play`, `challenge:read`, `challenge:write` (+ `puzzle:read` for activity, optional).
- **Board API** (not Bot API) is the only sanctioned way for humans to play from a third-party client; anything else risks bans. **No engine assistance ever during lichess games** — not even an eval bar. Gambit ships no engine at all in v1, eliminating the risk by construction.
- **Time controls**: lobby seeks = Rapid/Classical/Correspondence only. Blitz only via direct challenges / vs AI / bulk pairing. **No Bullet.** Surface this in the UI.
- **Play loop**: `GET /api/stream/event` (account events: challenges, gameStart/gameFinish), `POST /api/board/seek` (**HTTP connection must stay open — closing cancels the seek**), `GET /api/board/game/stream/{id}` (`gameFull` snapshot then `gameState` deltas, ndjson), `POST /api/board/game/{id}/move/{uci}`; chat/abort/resign/draw/takeback endpoints exist. Moves are **UCI**; client tracks board state itself → embedded C# chess rules required.
- **Opponents**: a Board API player is a normal lichess player — seeks match anyone on lichess.org/mobile/any client. `POST /api/challenge/{username}` (direct, blitz OK), `POST /api/challenge/open` (works even unauthenticated; returns `url`/`urlWhite`/`urlBlack` — but those games are played on lichess.org, not through the API), `POST /api/challenge/ai` (Stockfish 1–8, requires auth).
- **Spectating/TV**: `GET /api/tv/channels`, `GET /api/tv/feed` + `GET /api/tv/{channel}/feed` (**no anti-cheat delay**). `GET /api/stream/game/{id}` for arbitrary games is delayed ~3 moves/3–60s — note in UI.
- **Puzzles**: `GET /api/puzzle/daily|next|{id}` to fetch (solutions included in JSON for local validation). **No endpoint to submit solutions** — solving in Gambit can't affect the user's lichess puzzle rating; say so in the UI. Full puzzle DB is downloadable (CC0) if offline puzzles are ever wanted.
- **Anonymous play (confirmed)**: **No API path exists for unauthenticated move-making** — all `/api/board/*` and `challenge/ai` require a token. Fallback (adopted): play locally in-client, then **`POST /api/import`** with the PGN (form field `pgn`) — **works unauthenticated, 100 games/hour anon (200 authed), returns a shareable lichess game URL** with analysis board. Anonymous users can also spectate TV/games freely.
- **Rate limits & streaming gotchas**: adaptive limits; one request at a time; on 429 back off a full 60s (build a single-flight request queue). ~8 concurrent streams/IP. ndjson streams emit a **blank keep-alive line every ~7s** — tolerate blanks, no idle timeout <10s, auto-reconnect and resync from `gameFull`. In .NET: `HttpCompletionOption.ResponseHeadersRead`, infinite timeout on streaming requests.

### CC0 assets (license-verified)

- **3D pieces (later upgrade)**: Poly Haven "Chess Set" by Riley Queen — https://polyhaven.com/a/chess_set (CC0, glTF/FBX, full board+pieces). Backups on OpenGameArt (all CC0): /content/chess-pieces-0, /content/3d-chess-pieces, /content/chess-set-1, /content/chess.
- **2D glyphs (floor pops, UI icons)**: portablejim 2D chess set on FreeSVG (CC0) — https://freesvg.org/portablejim-2d-chess-set-pieces; also OGA "Chess Pieces and Board squares" (CC0). **Do NOT use the Cburnett/Wikipedia/lichess piece set — it is CC-BY-SA/BSD, not CC0.**
- Kenney has no chess pack. Record asset provenance in `Assets/ATTRIBUTION.md` (even for CC0, for auditability).

### Lichess asset licensing (lila `COPYING.md` is the single authority; no per-directory licenses)

Lichess is libre (AGPL code) but its bundled art/sounds carry per-set licenses:

- **Usable, CC0**: the **rhosgfx** 2D piece set — CC0 1.0, no conditions. This is the adopted floor-glyph source (D6).
- **Usable with attribution (not CC0, so outside our self-imposed constraint but available)**: chessnut (Apache 2.0), fantasy/spatial/celtic (MIT), kiwen-suwi/firi/totoy/papercut (CC BY 4.0), the MIT "Staunton-Pieces" 3D renders (PNG sprites, not meshes).
- **Copyleft strings attached**: cburnett/mono/merida (GPLv2+), mpchess (GPLv3+), pirouetti/letter/pixel (AGPLv3+), shapes (CC BY-SA 4.0).
- **NOT usable**: all CC BY-NC-SA sets (horsey, staunty, maestro, california, anarcandy, …) — non-commercial; alpha/chess7/companion/leipzig/reillycraig/riohacha (no reuse license — effectively lichess-only); the lichess logo.
- **Sounds: none usable for us.** The classic lichess move/capture/notify sounds ("standard" set and most others) are explicitly non-free/lichess-only; no set is CC0; only Enigmahack's futuristic/nes/piano/sfx are reusable and those are AGPLv3+. Gambit keeps its own synthesized sounds (`scripts/gen_sounds.py`) and can add new CC0 clicks (e.g. Kenney audio packs) if wanted.

## Architecture decisions

**D1 — Anonymous play: two players at one board (user choice).** A station gains two seats. `ChessStation` replaces single `OccupantSteamId` with `[Sync(FromHost)] WhiteSeatSteamId` / `BlackSeatSteamId` (+ names); `[Rpc.Host] RequestEnter(seat)` claims a seat first-request-wins; two camera anchors per station (one per board side, pitched ~35° down over the board). First sitter picks/gets White and sees "waiting for opponent"; a second player walks up and presses Use on the other side to take Black; game starts. Turn enforcement: only the seat whose turn it is has input; the mover's client validates the move with the embedded rules and sends `[Rpc.Broadcast] NetChessMove(uci, fenAfter)`; host folds latest FEN into `[Sync] BoardFen` for late joiners. **Lichess games use one seat only** (occupant + remote opponent); the second seat is disabled while a lichess game runs. After a local game: build PGN, `POST /api/import`, show the lichess URL + copy button to both players. No engine ships in v1 (also kills any fair-play risk); "vs AI" for logged-in users comes via lichess `challenge/ai` instead.

**D2 — Chess rules: vendor pure-C# chess sources behind our own wrapper.** s&box compiles game code from source with an API whitelist — NuGet DLLs can't be referenced. Primary: vendor Geras1mleo "Gera Chess Library" (MIT) into `Code/Chess/Vendor/` (keep MIT headers); expect light patching for whitelist friction (regex, events). Fallback (decide during M2): a compact ~700-line move-gen/validator (8x8 array, pseudo-legal + king-safety, castling/en-passant/promotion, FEN I/O, minimal SAN for PGN). Either way all callers use `Code/Chess/ChessGame.cs` (`ApplyUci`, `LegalMovesFrom`, `Fen`, `Pgn`, `Result`) so the vendor choice is swappable. Correctness gate: perft positions run as an in-game debug command on the user's machine.

**D3 — Auth: PKCE designed-in, token-paste guaranteed.** `Api/LichessAuth.cs` implements full PKCE. Two sandbox unknowns to spike on the user's machine at M3 start: can s&box open a system browser, and can game code bind a localhost listener for the redirect (likely no)? If loopback is impossible, the shipped flow is: show/copy the pre-filled token-creation URL `https://lichess.org/account/oauth/token/create?scopes[]=board:play&scopes[]=challenge:read&scopes[]=challenge:write&description=Gambit`, user pastes the `lip_` token into the splash screen. Token stored in `PlayerData` (FileSystem JSON) with the same hygiene as the old GUID: never networked, never logged unredacted. On 401: clear token, re-prompt.

**D4 — NDJSON streaming is the project's biggest risk — spike it first.** Board API play requires long-lived ndjson streams and a held-open seek request. Whether s&box's `Http` exposes incremental `ReadAsStreamAsync` (vs buffering the whole response) is undocumented. M3 begins with a ~50-line spike streaming `/api/tv/feed` (no auth needed) that the user runs before any dependent work. If streaming is impossible there is no Board API fallback (lichess has no polling API) — mitigation would be a Facepunch whitelist request; TV/puzzles/import (non-streaming-critical) are sequenced to still be shippable.

**D5 — Pieces: procedural primitives first, Poly Haven swap later (user choice).** `Code/World/ChessSetBuilder.cs` builds each piece type from tinted `models/dev/box.vmdl` boxes (pawn 2 boxes … king 4), matching the codebase's all-procedural aesthetic and the `AddBox` pattern. `BuildPiece(type, color, scale)` returns a GameObject and first tries `Model.Load("models/chess/{type}.vmdl")` with procedural fallback — so importing the Poly Haven set on the user's machine later is a drop-in, one-function swap. Board = 64 tinted cell boxes + rim on a table.

**D6 — Floor pops: flat 2D glyphs via shader glyph atlas (user choice).** Keep `FloorCheckerboard.cs`'s slab, checker bake, and `Repick()` spacing/round-robin logic. Changes: pops occur on **both** square colors (drop the whites-only parity filter), glyph color is **opposite** the square (white glyph on dark cell, dark glyph on light cell). Glyph art: **lichess's own "rhosgfx" piece set — the one lichess set that is CC0 1.0** (per lila `COPYING.md`; most other lichess sets are GPL/CC-BY-SA/NC or lichess-only — see "Lichess asset licensing" below). Fallback: portablejim CC0 SVG set. Mechanism: rasterize the CC0 SVGs into a 6-glyph alpha atlas texture (`Assets/textures/chess_glyphs.png`, generated by a small Python script in `scripts/` so it's reproducible); extend the popmap texture so each cell encodes glyph index (R channel, 0=none/1–6=piece); rewrite `floor_checker.shader` to sample the atlas within a popped cell and blend the glyph over the checker color, tinted by parity. Keep bevel/border/roughness attributes. **This needs in-editor shader iteration — flagged as a user-machine-heavy task in M6.**

**D7 — Networking split.** s&box lobby (presence, seats, chat, station rebuild) keeps `LobbyNetworkManager` near-verbatim (delete the `TargetUrl` backend-switcher; keep admin claims + station-count rebuild + host migration). Each player's lichess game streams on **their** client with **their** token — tokens never cross the wire. Spectators see boards via `[Rpc.Broadcast]` relay of `(fen, lastMoveUci, wClockMs, bClockMs, whiteName, blackName)` — direct analog of the old `NetBoardStart/NetBoardSync/NetBoardClear` — with host-folded `[Sync] BoardFen` for late joiners.

**D8 — HTTP allowlist**: set `"HttpAllowList": ["https://lichess.org/"]` in `gambit.sbproj` (covers `/api/*`, `/oauth`, token, import). If s&box rejects it at publish, fall back to `null` with a comment.

## File mapping (rotaliate → gambit, all under `Code/` unless noted)

**Keep near-verbatim** (namespace rename only): `World/LobbyNetworkManager.cs` (minus TargetUrl switcher), `World/LobbyPlayer.cs`, `LobbyRoom.cs`, `FacePlayer.cs`, `RoomLightOrbit.cs`, `Streetlights.cs`, `MarqueeGlow.cs`, `DiscordButton.cs`, Info/Settings walls + stations + wall razor panels, `Game/GamepadBinds.cs`, `Audio/SoundPlayer.cs` + all sounds (tick/tock→clocks, pop→captures, servo→slides), `UI/ChatPanel.razor`, `LobbyOverlay.razor`, `SettingsScreen.razor`, `InfoScreen.razor`, `NameTagPanel.razor`, marquee number panel (board number), `Editor/HotloadRebuild.cs` (retarget types), `Libraries/gamah.skafinity` (music), `Assets/shaders/floor_checker.shader` (until M6 glyph rewrite), `Assets/scenes/lobby.scene` (user re-wires renamed components in-editor once — documented M1 step).

**Transform**:
- `World/ArcadeRing.cs` → `World/ChessRing.cs` — keep ring math/preview/network-spawn/slide-rebuild/screen-rect UI math; `BuildCabinet()` → `BuildChessTable()` (table, board frame, 64 cells, `ChessSetBuilder` pieces at start position, two camera anchors).
- `World/ArcadeStation.cs` → `World/ChessStation.cs` — keep occupancy RPC pattern but two seats (D1), camera lock flow, relay-tap events; new relay payloads (D7); delete demo/attract replay (Go-backed; "TV attract" is stretch) and GUID `ValidateIdentity`.
- `World/FloorCheckerboard.cs` — pops → glyph popmap (D6).
- `Game/PlayerData.cs` — drop GUID/Steam-link identities; add `LichessToken` (never logged/synced) + `LichessUsername`; keep cosmetic settings.
- `Theme/Colors.cs` — colorblind palettes → board/piece themes.
- `UI/ArcadeScreenPanel.razor` → `UI/StationScreenPanel.razor` (same screen-router pattern, floating above the table).
- `UI/Screens/SplashScreen.razor` — rewrite: "Sign in with lichess" (PKCE or token paste) / "Play anonymously" (display name only).
- `UI/Screens/ModePickerScreen.razor` — rewrite: Seek (rapid/classical, rated toggle) / Challenge lichess user / Challenge AI 1–8 / Open-challenge link / Puzzles / Watch TV; anonymous users see local-board play + puzzles + TV only.
- `UI/GameHud.razor` — rewrite: clocks, SAN move list, draw/resign/abort, opponent name+rating, chat (lichess chat online, s&box chat local), game-over + import-link panel.

**Delete** (Go-backend era): `Api/ApiClient.cs`, `Api/Models.cs`, `Ws/WsClient.cs`, `Game/MultiplayerController.cs`, `GameController.cs`, `Board.cs`, `PlayerStats.cs`, `Achievements.cs`, `World/CubeBoardView.cs`, `RemoteBoard.cs`, `LeaderboardWall.cs`, `LeaderboardStation.cs`, `UI/WallLeaderboardPanel.razor`, `UI/Screens/MultiplayerScreen.razor`, `LeaderboardScreen.razor`, demo/button/rotate glyph panels, `scripts/gen_thumbnail.py`, Rotaliate thumbnails.

**New**:
- `Api/LichessApi.cs` — REST (account, seek, move, resign/draw/abort, challenges, puzzles, TV channels, import) with single-flight queue + 60s 429 backoff.
- `Api/LichessModels.cs` — DTOs. `Api/NdjsonClient.cs` — streaming reader (keep-alive tolerant, auto-reconnect/resync). `Api/LichessAuth.cs` — PKCE + token-paste (D3).
- `Chess/ChessGame.cs` wrapper, `Chess/Vendor/` (or `MoveGen.cs` fallback), `Chess/Pgn.cs`, `Chess/Clock.cs` (interpolating display clock).
- `Game/LichessGameController.cs` — state machine Idle→Seeking→Playing→GameOver; owns event+game streams, applies `gameState`, posts moves, clocks, chat.
- `Game/LocalGameController.cs` — anonymous two-seat local game + PGN import. `Game/PuzzleController.cs` — fetch/solve-locally.
- `World/ChessBoardView.cs` — renders `ChessGame`/FEN on station board: piece placement, move lerp, capture removal, last-move + legal-move highlights, promotion picker; cursor-ray cell picking while seated.
- `World/ChessSetBuilder.cs` (D5), `World/SpectatorBoard.cs` rewrite (giant wall board: featured station mirror + lichess TV mode).
- `Assets/textures/chess_glyphs.png` + `scripts/gen_glyph_atlas.py` (M6).

## Milestones (future sessions; every gate is user-tested in-editor)

- **M0 — Repo bootstrap** *(done)*: copy, strip, rename, PLAN.md, allowlist, commit, push private. Gate: project opens and compiles in the user's s&box editor. Compile strategy: delete dead call sites rather than stubbing.
- **M1 — Chess board world** *(done — gate passed 2026-07-13)*: `ChessRing.BuildChessTable`, `ChessSetBuilder`, `ChessStation` two-seat occupancy + camera lock (no game logic — sit down at either side, see a set-up board, stand up). Legacy Go-backend gameplay code deleted per the file mapping above. User re-wires `lobby.scene` components once — see "M1 scene rewire" below. Gate: walk lobby, 8 boards render with pieces, both seats work, slide-rebuild works.
- **M2 — Local anonymous chess** *(code-complete 2026-07-13, gate pending user test)*: vendor/patch chess lib (or fallback) + perft debug command; `ChessBoardView` input/render; `LocalGameController` + seat/turn RPCs; FEN spectator relay + late-join; PGN build + `POST /api/import` (first lichess call — validates allowlist); minimal GameHud. Gate: two clients play a full legal game at one board, third client spectates, import link opens on lichess. Built: vendored Gera Chess d4f3f69 (MIT) patched per D2 (perft + upstream tests + wrapper tests pass in a dotnet harness on the dev host; `gambit_perft` re-checks in-sandbox); table lifecycle is host-driven Idle→Playing→Over on the per-station `LocalGameController`. **Known issue (carry into M3):** the game-over "Get lichess link" button does not work — after checkmate (and presumably resign/leave, any game end) nothing lands on the clipboard. This is the FIRST real lichess call, so the likely culprits are the `POST https://lichess.org/api/import` failing (HttpAllowList not actually covering the POST, or s&box `Http.RequestAsync` not sending the form-urlencoded `StringContent` body as expected — try the documented `Http.RequestAsync(url,"POST",content,headers)` 4-arg form) so `LichessUrl` never gets set and the copy button never appears; or `Clipboard.SetText` no-opping in the engaged-HUD context (DiscordButton.Copy uses the same call and reportedly works, so compare). Import/copy code is `LocalGameController.ImportToLichess` / `CopyLichessUrl`; button wiring is `UI/GameHud.razor`. Fixing it also completes the M2 gate's "import link opens on lichess" step and validates the allowlist (D8).
- **M3 — Lichess auth** *(code-complete 2026-07-13, gate pending user test)*: **First, carry-in from M2:** fix the broken game-over lichess import (the "Get lichess link" button copies nothing — see the M2 line above). It is the first live lichess request, so debugging it here also validates the HttpAllowList before the auth/streaming work depends on it. Then: Spike 1 FIRST: ndjson streaming via `/api/tv/feed` (D4 — project-critical). Spike 2: browser-open + loopback feasibility (D3). Then `LichessAuth`, token storage, SplashScreen, `GET /api/account` name/rating in name tag. Gate: sign in, token persists across restart, 401 → re-prompt.
  **Built (M3):** all lichess REST now goes through `Api/LichessApi.cs` — one seam owning the CLAUDE.md rate discipline (single-flight gate + 60s-429 backoff) and, the **actual fix for the dead M2 import**, an `Accept: application/json` header on every request (confirmed in-editor: without it, POST /api/import returns HTTP 200 + the game's HTML page instead of {id,url}; the 3-vs-4-arg request shape was a red herring, and /api/account worked regardless because it is JSON-only). `Api/LichessModels.cs` DTOs, `Api/LichessAuth.cs` (token-paste sign-in → validate via `GET /api/account` → store token+username+rating in `PlayerData`; sign-out revokes via `DELETE /api/token`; `OnUnauthorized` clears+re-prompts; `ValidateStoredToken` re-checks on load), `UI/Screens/SplashScreen.razor` (screen-space modal self-attached like GameHud; copy-scoped-token-link + paste flow, or anonymous display-name; cursor freed via `UseLookControls=false`+`UseInputControls=false`, restored on close), name tag shows lichess name+rating (`[Sync] GambitRating`). Sign-in ships **two paths**, both converging on `SignInWithToken` (validate via `/api/account`, store): **personal-token paste is the primary/default flow** (the cleanest UX — the pre-scoped token-create page, then paste the `lip_`), and **OAuth2 Authorization-Code + PKCE (code-paste)** is the "advanced" option (`Api/LichessOAuth.cs`, confirmed working end-to-end in-editor). **Spike verdicts:** D3 spike 2 — a fully-automatic OAuth is impossible here: no browser-open API, the loopback *listener* that would auto-catch the redirect is outside the whitelist (`HttpListener`/sockets → SB1000), and **lichess has no OAuth device-flow** (`/api/oauth/device` and `/oauth/device` both 404 — verified), so *some* manual paste is unavoidable. The OAuth flow: copy the authorize link, authorize, paste the address-bar URL back (`redirect_uri=http://localhost/…`, which fails to load but carries `?code=` in the bar) — `LichessOAuth.Complete` verifies `state`, exchanges the code at `POST /api/token`, stores the bearer. PKCE needs S256; rather than risk `System.Security.Cryptography`/`Convert.ToBase64String`, **SHA-256 and base64url are hand-rolled in pure integer math** (verified on the dev host against System crypto, the framework base64 encoder, and the RFC 7636 vector) — zero whitelist exposure. A `UseS256=false` toggle exists to test whether lichess accepts `plain` (unhashed) challenges. **Bonus goal (deferred):** make the OAuth paste painless with a tiny static callback page hosted on `gamah.net` that JS-reads `?code=` and shows a one-click "Copy code" button, so `redirect_uri` lands somewhere clean instead of a connection-refused localhost page. **D4 spike 1 RESULT (in-editor): incremental streaming does NOT work** — `await Http.RequestAsync` on the endless TV feed never returns (buffers the whole body), so incremental reads of held-open Board API streams are unavailable via this path. *(Worked around in M4 by **polling** `account/playing` instead of reading the game stream — see M4b; a headers-first `Http` read from Facepunch would still be the clean upgrade to real-time.)* `gambit_lichess_tv` reproduces it. Console commands (dev): `gambit_lichess_tv`, `gambit_lichess_import_test` (self-tests the POST with a known-good PGN, isolating HTTP from our PGN builder), `gambit_oauth` + `gambit_oauth_complete <redirect-url>` (headless PKCE flow), `gambit_signin [token]`, `gambit_signout`, `gambit_whoami`. Token hygiene: only ever in the Authorization header, `LichessApi.Redact` for logs, never `[Sync]`/RPC'd. **M3 gate (user, in-editor):** `gambit_lichess_import_test` now returns JSON with a url (after the Accept-header fix — the raw HTTP was already 200; only the reply format was wrong) → then a real game-over "Get lichess link" works, closing M2's import step; open splash (auto on first run, or `gambit_signin`), paste a `lip_` token, sign in, name tag shows the lichess name+rating; restart → still signed in; revoke the token on lichess then reload → 401 clears it and re-pops the splash.
- **M4 — Board API play** *(DONE — merged to master 2026-07-14)* — shipped in two passes: **M4a (browser-link flow) + M4b (in-sbox play by polling + presence)** merged first (PR #2, squash `472e36d`, 2026-07-13); the **M4 remaining work — quick-play seek (#1), spectator relay (#2), head-to-head (#3), the host-visibility bug fix, and a display-name unification** merged from `m4-seek-relay-h2h` (2026-07-14). Gate: play a rated rapid game vs a random lichess opponent from inside the lobby while another client watches — **code-complete; the seek/relay/AI paths are exercisable solo, the two-authed head-to-head handshake and the #0 presence check await a live second account (see "M4 — remaining").** Everything is polling-based; no relay or engine fix was needed (risk 1), and M5+ don't need one either (see M5).
  **M4a — open-game link flow (DONE, merged).** Full Board API *streaming* play stays blocked (see risk 1 below — corrected finding), so M4's first slice ships the play path that needs **no** streaming and **no** hosted infra: `POST /api/challenge/open` (unauthenticated → always casual/**unrated**, **Rapid 10+0** = `clock.limit=600&clock.increment=0`). Walk up to a side and sit (you now take the side you actually walked to — the old D1 "first sitter = White" override is dropped, `LobbyPlayer.FindNearbySeat`), press **Create Rapid 10+0 game**, and you get the two colour-pinned URLs lichess returns (`urlWhite`/`urlBlack`, public → safe to `[Sync]`): your side's URL to open in a **browser** to play, and the opponent's URL to share. A second client sitting the other seat sees the same game (synced), or anyone **pastes a link** and the camera swoops to the side `?color=` assigns (`LichessGameController.SeatFromUrl` → `LobbyPlayer.JoinLichessSide`). The chess is played on lichess.org; sbox is the meeting point (side assignment + link handoff + camera routing) — **sbox↔browser play**, no moves-on-the-board yet. New: `Game/LichessGameController.cs` (per-station, added by ChessRing beside `LocalGameController`; `LocalGameController` auto-start is suppressed while a board hosts an open game). `Api/LichessApi.CreateOpenChallenge`, `LichessOpenChallenge` DTO, `GameHud` lichess panel + join-by-link `TextEntry`. Console: `gambit_open_challenge` (self-test, logs raw reply to confirm URL shape), `gambit_join <url>` (guaranteed-working twin of the paste field). **M4a gate (user, in-editor):** `gambit_open_challenge` returns `urlWhite`/`urlBlack` → sit at a side, Create Rapid 10+0, copy your link, open it in a browser and play (unrated, rapid, correct colour) → a friend/2nd browser opens the shared link and the game is live → a 2nd sbox client sits the other seat (or pastes the link) and the camera moves to the API-assigned side.
  **M4b — in-sbox play by polling + presence (DONE, merged).** Real moves-on-the-board play *without* streaming and *without* a relay. Reads: poll `GET /api/account/playing` (~1.5s) for each ongoing game's `fen`/`lastMove`/`isMyTurn`/`secondsLeft`. Writes: `POST /api/board/game/{id}/move/{uci}` (optimistic local apply, reconciled on the next poll; polling is gated so it never races an in-flight move). Resign: `POST /api/board/game/{id}/resign`; game-over is detected when the game drops out of `account/playing`, then `GET /game/export/{id}` supplies the result. Fine for Rapid/Classical; **can't do bullet** (round-trip latency + rate limits, and lichess bars bullet on the Board API — risk 1). Three ways to start from the seated "Play in sbox" panel (all sign-in required, you play the side you sat at, Rapid 10+0):
    - **Challenge & play here** — `POST /api/challenge/{user}?color=<seat>`; the named user accepts (on lichess.org or their own sbox client) and the poll takes it live. Needs a 2nd identity.
    - **vs Stockfish** — `POST /api/challenge/ai` (starts instantly, zero setup).
    - **vs anonymous browser** — `PlayOpenGame`: create an authed open challenge, **self-seat via `POST /api/challenge/{id}/accept?color=<seat>`** (the accept endpoint's `color` query is documented "only valid if this is an open challenge" — no browser needed on our side), then share the *opponent* URL to any browser incl. anonymous. `SeatUrl` is retained only as a fallback if the accept ever fails.
  New: `Game/IBoardGame.cs` (render/drive abstraction — `ChessBoardView` renders the local game or a lichess game through it, M2 behaviour byte-identical when no lichess game is live) and `Game/LichessPlayController.cs` (per-station, **local-only**: each client polls with **its own** token, nothing `[Sync]` — token never crosses the wire, D3). `LichessApi` gains `ChallengeUser`/`ChallengeAi`/`AcceptChallenge`/`GetAccountPlaying`/`BoardMove`/`BoardResign`/`CancelChallenge`/`GameExport`/`HoldGameStream` (+ DTOs). **Presence:** while a game is live the client holds `GET /api/board/game/stream/{id}` open **unread** (bypassing the single-flight gate) so lichess doesn't flag the player as disconnected — without it the opponent's client shows "left the game" after every move and can claim victory. The stream carries every move but `Http` can't read it mid-game (risk 1), so it's presence-only (`EnsurePresence` re-arms it if it drops, with a cooldown; cancelled on game-over/resign/new-game/destroy). `GameHud` gains the "Play in sbox" section + in-game turn/clock/resign panel; console `gambit_challenge <user>`, `gambit_challenge_ai [level]`, `gambit_play_open`. **Confirmed in-editor:** lichess accepts Board-API moves on a game seated outside the Board API, so the whole loop works; the open-challenge self-seat via `accept?color=` also works (an earlier GET-the-seat-URL attempt did not and was removed). **Net: the relay is needed for nothing in this feature set** — its only conceivable value is lower-latency *streaming* for blitz (polling covers rapid/classical; bullet is barred; 3rd-client spectating can go over sbox `[Rpc]`). **M4b gate (user, in-editor):** signed in, sit, `gambit_challenge_ai 3` → play out a game vs Stockfish on the board; then **vs anonymous browser** → open the opponent link in an incognito window → the board goes live and you play both sides. **M4 — remaining (all DONE, merged from `m4-seek-relay-h2h` 2026-07-14).** **#1 (seek), #2 (spectator relay), #3 (head-to-head), the host-visibility bug fix, and a display-name unification** all landed — together they close the original gate (random opponent + a watcher) and add authed-vs-authed play. The build compiles clean in-editor (user, 2026-07-14). **Still awaiting a *live* confirm** (need a second lichess account / players, so deferred by the user, not blocking the milestone): the full #3 two-authed handshake, and **#0** (presence — that the opponent no longer flips to "left the game"). **#4 (secondary — draw/abort, rated toggle, incoming-challenge-from-a-stranger via `GET /api/challenge`, lichess chat) explicitly slipped to M5/M6.** Note the #3 accept path only covers the *seated-across* handshake (challenger's client hands the exact challenge id to the opponent's client over `[Rpc.Broadcast]`); a *general* inbound challenge from anywhere is the slipped #4 item.
    **M4 gate (user, in-editor):** signed in + seated, hit **Quick match — rated** (or `gambit_seek`) → you're paired with a random lichess player, the game goes live on the board, and you play it out (camera flips to your assigned side if it differs from the seat); meanwhile a **second client** near that table watches the pieces move in real time (~poll latency) via the relayed FEN, with no local game starting on top.
    - **0. Verify the presence fix** (pushed with M4b but *not yet confirmed in-editor*): play a game from sbox and confirm the opponent no longer flips to "left the game" after moves / can't claim victory. If it still shows disconnected, also hold the account **event stream** (`/api/stream/event`) open with the same held-unread pattern as the game stream — that's the only other presence lever.
    - **1. Quick-play seek (random opponent, rated)** *(code-complete on branch `m4-seek-relay-h2h`, gate pending user test)* — the "random lichess opponent" gate item. Built as designed: `LichessApi.HoldSeek` POSTs `/api/board/seek` (form `rated`/`time`(min)/`increment`(s)/`variant`) held **open** — like `HoldGameStream` it bypasses the single-flight gate and ignores the buffered body — and `LichessPlayController.QuickSeek(rated)` fires it, then the existing poll (`account/playing`) picks up the pairing. A pre-seek snapshot of ongoing game ids (`_preexistingIds`) makes the poll adopt only the **new** game (`_expectAny` path in `FindOurGame`). Colour is lichess's to assign, so `AdoptSide` sets `_myColor` from the game's `color` and, if it differs from the seat sat at, swoops the camera to the real side via `LobbyPlayer.JoinLichessSide`. UI: "Quick match — rated / casual" buttons + a seek-aware "pairing…" note; console `gambit_seek [rated|casual]`. **Spike folded in:** the held-open empty-bodied seek returning-on-match (vs hanging) is the same mechanism `HoldGameStream` already proved in M4b, so no separate throwaway spike — but confirm in the gate that the seek pairs and the game goes live.
    - **2. Spectator relay (D7)** *(code-complete on branch `m4-seek-relay-h2h`, gate pending user test)* — the "another client watches" gate item. The playing client host-folds its polled FEN + player names through an `[Rpc.Host]` (`HostRelay`) into new `[Sync(FromHost)]` fields on `LichessPlayController` (`RelayLive`/`RelayFen`/`RelayLastMove`/`RelayWhite|BlackName`) — the analog of `LocalGameController.HostFold`, **FEN/names only, never the token (D3)**. Non-players reconstruct a read-only `ChessGame` from `RelayFen` (`SyncSpectator` → `_spectatorGame`) and render it through the existing `IBoardGame` path: `ChessBoardView.Source` now keys off `LichessPlayController.ShowsBoard` (= `Active` for the player **or** `Spectating` for watchers) instead of just `Active`. `LocalGameController.LichessBusy` also gained `BlocksLocalGame` (`Active || RelayLive`) so a relayed/played lichess game suppresses the two-seat local match on every client. Relay is torn down (`ClearRelay`) on game-over/clear.
    - **HOST-VISIBILITY BUG (found in gate testing 2026-07-13; fix code-complete on `m4-seek-relay-h2h`, pending in-editor confirm) — clients couldn't see the host.** With 3 instances up, the two *joined* clients saw each other's avatars/boards but **not the host's**. Root cause (traced): the host avatar was `NetworkSpawn`ed inside the host's *own* `OnActive`, which fires **during `Networking.CreateLobby`** — the host connection isn't fully settled yet, so the object never made it into the snapshot sent to later joiners (stations, spawned host-owned in `OnHostInitialize`, and joiner avatars, spawned from genuinely-remote `OnActive` calls, both replicate fine — only the host's own-connection spawn was too early). **Fix:** `LobbyNetworkManager.OnActive` now detects its own connection (`connection == Connection.Local`) and **defers** the clone+`NetworkSpawn` to the first `OnUpdate` (past the `CreateLobby` call stack, host connection now active); joiners still spawn inline. Spawn logic factored into `SpawnPlayer(Connection)`. Gate: host + 2 joiners → all three see each other's avatars **and** a relayed lichess game hosted by the host.
    - **Seating (done on `m4-seek-relay-h2h`):** sitting now physically plants the avatar at its side of the board facing it (`LobbyPlayer.BeginEngage` → `ChessStation.SeatWorldPosition`; networked from the owner, invisible locally since the avatar's hidden + camera locked), and standing up restores the pre-sit transform so the camera hand-back doesn't snap. Fixes the parent-project bug where seated players stood/animated wherever they walked up; with the body no longer moving, remote copies drop out of the walk cycle to idle.
    - **3. Two authed players head-to-head** *(code-complete on `m4-seek-relay-h2h`, gate pending user test)* — two signed-in sbox players at one board play a real lichess game vs each other. Built: each seat now syncs its occupant's **lichess username** (`ChessStation.WhiteLichessName`/`BlackLichessName`, set from `LichessAuth.Username` in `Enter`→`RequestEnter`→`SetSeat`; public info, safe to `[Sync]`, token still never crosses the wire). When **both** seats hold signed-in lichess players (`ChessStation.BothSeatsLichess`), `LocalGameController` **suppresses the local two-seat auto-start** (`SuppressLocalStart`) so the "PLAY IN SBOX" panel stays up instead of forcing an anonymous local game — otherwise head-to-head would be unreachable through the HUD. The challenger hits **Play <name> (head-to-head)** → `LichessPlayController.ChallengeSeatedOpponent()` reads the opposite seat's lichess name, `POST /api/challenge/{name}?color=<own seat>` (casual Rapid 10+0), then broadcasts `NetAskAccept(challengeId, oppSeat, myName)` (`[Rpc.Broadcast]`, same client→all pattern as `NetChessMove`). The seated opponent's client (only the one holding that seat, signed in + idle) auto-accepts that exact challenge (`AutoAccept` → `AcceptChallenge(id, color:null)` — lichess only lets them accept a challenge it actually sent them, so it's self-verifying); both then fall into the existing `account/playing` poll loop (matched by opponent username, colours already = seats, so no camera swoop). Neither player leaves sbox. HUD button + `gambit_challenge_seated` console cmd. **Gate:** two signed-in clients sit opposite → each sees "Play <other> (head-to-head)" → one clicks → both boards go live and they play a real (casual) lichess game against each other, standing up resigns. (Rated toggle is #4 secondary.)
    - **State reset (done on `m4-seek-relay-h2h`):** standing up from the seat now tears the in-sbox lichess controller back to Idle in every phase — `LichessPlayController.LeaveSeat()` (wired into `LobbyPlayer.RequestLeave`, and armed by the same two-stage confirm as the local game) resigns a live game, cancels a pending challenge/seek/open link, or clears the game-over screen; the host also auto-recycles the spectator relay when the table empties (mirrors `LichessGameController`), covering an abrupt disconnect. Dev cmd `gambit_play_reset`. So a finished game / an opponent bailing (lichess ends it → poll → Over → Done or leave) no longer strands the board. **Anonymous play stays browser-only:** every `/api/board/*` + `challenge/ai` needs a `board:play` token (verified 2026-07-13: unauth seek → 400 "Authentication required", unauth `challenge/ai` → 401 "Missing authorization header"); lichess's own anon web play uses a session cookie, not an obtainable token, so there is no unauthenticated in-sbox play path — quick-match/AI stay sign-in-gated.
    - **4. Secondary (finish M4 or slip to M5/M6):** draw offer/accept + abort-before-first-move (`/api/board/game/{id}/draw/yes`, `/abort`) — resign is done; rated toggle on the challenge/seek UI; incoming-challenge handling (someone challenges *you* — poll `/api/challenge` or the event stream → accept); clocks are coarse (poll gives only *your* `secondsLeft`, so both-sides live clocks need the unreadable stream); lichess online chat (lowest priority).
  **Constraints (carry forward):** no incremental stream read → polling (moves ~1.5s latency, coarse clocks); **bullet impossible** (barred by lichess + latency); the held game stream is presence-only (unreadable mid-game). The real-time upgrade — one readable stream that replaces polling — needs either a **Facepunch headers-first `Http` feature request** (clean fix, already the D4 mitigation) or a **relay** reading the lichess stream server-side and pushing over `Sandbox.WebSocket` (incrementally readable); both are optional, and the relay is otherwise needed for nothing in this feature set.
- **M5 — Spectate / TV / puzzles** *(code-complete on branch `m5-m6-spectate-puzzles`, gate pending user test)*: SpectatorBoard wall rewrite (featured station + TV); puzzles (`daily`/`next`, local solve with retry/reveal, "doesn't affect your lichess puzzle rating" copy); watch a specific game by ID (delay noted in UI). Gate: TV on the wall; puzzles solvable at any board.
  **Built (M5):**
    - **Chess wrapper:** `ChessGame.TryFromPgnAtPly(pgn, ply)` / `TryFromPgn(pgn)` reconstruct a position from movetext via the vendor PGN parser (survived the whitelist patch — regex → hand-written scanners) + `MoveIndex` navigation → clean FEN. Feeds both puzzles and TV.
    - **API/models:** `LichessApi.GetPuzzleDaily/GetPuzzleNext/GetPuzzleById`, `GetTvChannels`, `GameExportPgn` (an `Accept: application/x-chess-pgn` override on `Send` — the one non-JSON call, so the vendor can parse the body). DTOs `LichessPuzzleResponse`/`PuzzleData`, `LichessTvChannel`. All request/response → they ride the existing single-flight + 429 gate untouched.
    - **Puzzles (`Game/PuzzleController.cs`, IBoardGame):** per-station, **client-local** (nothing synced) like `LichessPlayController`; `ChessBoardView` renders/drives it with zero new branching (`Source` now prefers `Puzzle.Active`). Loads a puzzle, plays `solution[0]` (the opponent's setup move — lichess convention: the position is one ply before the solver's turn), sets the solver on move, auto-plays each opponent reply after a beat; wrong-but-legal moves snap back with a "not the move" flash (retry), Reveal auto-plays the line, Next/Retry/Close in the HUD. Orients the board to the solver's colour (`JoinLichessSide`). Anonymous-friendly (daily needs no auth). `SuppressLocalStart` gains a puzzle check; `LobbyPlayer.RequestLeave` resets it. HUD: puzzle panel + "PUZZLES" entry (Daily/Random) in the idle board panel. Console `gambit_puzzle [daily|next]`.
    - **Spectator wall (`World/SpectatorWall.cs` + `SpectatorStation.cs` + `Game/SpectatorController.cs`):** a giant west-wall display board, **self-provisioned** by `LobbyRoom.EnsureSpectatorWall` (like the ring — no scene rewire) and hotload-registered. Three sources, all non-streaming: **Featured** — mirror any live sbox table straight off the M4 `[Sync]` relay / host-folded local FEN (real-time, zero API, works on every client); **Lichess TV** — poll `/api/tv/channels` (plain JSON) → pick a channel → `GameExportPgn` → reconstruct FEN (coarse latency); **Watch by ID** — same export poll for a pasted id/URL. The board renders as a **real 3D chess set** (`World/SpectatorBoard3D.cs` — the same `ChessSetBuilder` meshes the tables use, driven by the controller's FEN, with a capture/slide animation, a last-move highlight, and its own raking `SpotLight` so the pieces cast shadows; `SpectatorWall` builds it flat and stands it up on the wall with a slight back-tilt). This replaced an earlier 2D WorldPanel whose glyph-atlas pieces would not paint in-editor — meshes render reliably and, unlike panel CSS, can be reasoned about without the editor. A **scoreboard** WorldPanel floats above it (`UI/SpectatorScorePanel.razor`) with both players' names, titles, ratings, and a live clock — parsed from the export PGN headers + `%clk` comments (`clocks=true`; the vendor strips `{…}` comments so FEN reconstruction is unaffected), the ticking side accented; the sbox-table featured source has names only. The channel picker (`UI/Screens/SpectatorScreen.razor`) is a screen-space ScreenPanel gated on `SpectatorStation.Active`, self-attached at runtime (`LobbyPlayer.EnsureSpectatorScreen`) — engaging frees the cursor (no mouselook conflict). `LobbyPlayer` gained a `SpectatorStation` engage path (`NearbySpectator`/`EngageSpectator`/`BoardEngaged`/`Disengage`); LobbyOverlay shows "Press E to spectate games". Console `gambit_watch <id|url>`.
    - **Spike (as required):** `gambit_tv` fetches `/api/tv/channels`, picks the rapid channel, exports its PGN and logs the reconstructed FEN — **run it before trusting the wall's lichess-TV path** to confirm the export is current-enough (re-run a few seconds apart to gauge the fair-play delay). The featured-sbox-mirror path needs no spike (it's the proven M4 relay).
    - **Streaming still not needed:** featured mirror is the M4 relay; puzzles/TV/watch are request/response. A relay or a Facepunch headers-first `Http` read would only smooth/real-time the TV path.
  **M5 gate (user, in-editor):** sit at a board → **Daily puzzle** (or `gambit_puzzle`) → solve it by clicking moves (wrong move flashes + snaps back; Reveal auto-plays; Next loads another); the copy says it doesn't affect your lichess rating. Then walk to the **west wall**, press E → **Show a live table** mirrors a game happening at a table (start one on a 2nd client to see it move in real time), and **lichess TV** (after `gambit_tv` confirms a current FEN) / **Watch** a pasted game id show a live-ish position with the delay noted.
  **Streaming is not a prerequisite (settles the "are we blocked?" question):**
    - **Puzzles** — pure request/response (`GET /api/puzzle/daily|next|{id}`, solutions embedded for local validation). Zero streaming. The safest place to start M5.
    - **Featured-station mirror** — mirroring a live game happening at an *sbox* table onto the wall board rides the **existing M4 `[Sync]` FEN spectator relay** (`LichessPlayController.Relay*` / `LocalGameController` fold). No lichess API at all.
    - **Lichess TV / watch-a-game-by-id** — the clean feeds (`/api/tv/feed`, `/api/stream/game/{id}`) are ndjson and stay blocked (risk 1), but the **non-streaming path works by polling**: `GET /api/tv/channels` is a plain-JSON snapshot (current featured game + players per channel), and the chosen game's position is polled the same way M4b polls `account/playing`. Result: **coarse-latency** spectating (snapshot-y, a few seconds behind), not real-time — degraded, not blocked. The lichess ~3-move anti-cheat delay on arbitrary ongoing games is a lichess policy to surface in the UI, not our limitation.
    - **Spike-first (same discipline as before):** a ~10-min console spike to confirm `/api/tv/channels` + a single-request game snapshot give a *current-enough* position before building the wall UI on them. The polling mechanism itself is already proven by M4b.
  A relay or a Facepunch headers-first `Http` read would only *improve* this (real-time smooth TV, live both-sides clocks, lower latency, blitz — bullet stays barred regardless); none of it gates M5 or M6.
- **M6 — Floor glyph pops + polish** *(code-complete on branch `m5-m6-spectate-puzzles`, gate pending user test; the shader needs in-editor iteration)*: glyph atlas + shader rewrite (D6, user-machine-heavy); optional Poly Haven piece import + swap; sound mapping; new thumbnail/branding; settings trim; rate-limit audit (single-flight + 429 everywhere); token-hygiene audit (grep logs/RPCs). Gate: full playtest.
  **Built (M6):**
    - **Floor glyph pops (D6 — FULL shader replacement, user chose this over additive):** `scripts/gen_glyph_atlas.py` rasterises the six Unicode solid chess pieces from the system **DejaVu Sans** font into a 6-cell alpha atlas `Assets/textures/chess_glyphs.png` (CC0-clean — our own raster, **not** the Cburnett/lichess set; provenance in `Assets/ATTRIBUTION.md`). The PNG is committed (generated + eyeballed on the dev host: K Q R B N P silhouettes, correct order). `FloorCheckerboard` now bakes a `PopMap` (plain checker colour) **plus** a `GlyphMap` (R = glyph index 0–6, one texel/cell); pops land on **both** square colours (dropped the whites-only filter), round-robin over the 6 piece types, spaced as before. `floor_checker.shader` rewritten: for a popped cell it looks the piece up in the atlas and blends it over the square in the **opposite** colour (white glyph on dark, dark on light), keeping the bevel/border/roughness. **Cannot be verified on this host — needs in-editor shader iteration (D6 was always flagged user-machine-heavy).** Safety: if the atlas fails to load, no glyph indices are written → a plain checker floor, never solid-square artefacts. If pops render as solid squares in-editor, the atlas didn't mount (`Texture.Load(FileSystem.Mounted, "textures/chess_glyphs.png")`) — re-import the PNG. The palette-coloured pop feature (arcade-era colourblind palettes) is retired for the floor; `ColorScheme` still themes the room elsewhere.
    - **Sound mapping:** move sounds now fire for **lichess games** too (were local-only): `LichessPlayController` plays pop-on-capture / tick-tock-by-side — 2D on your own board, positional when spectating a relayed table; puzzles likewise (`PuzzleController`). Servo slides (station rebuild) were already mapped in M2/M4.
    - **Rate-limit audit (clean):** only three `Http.RequestAsync` sites exist, all in `LichessApi`; the two held-open streams (seek, presence) deliberately bypass the single-flight gate (documented), everything else — including the new puzzle/TV/export calls — funnels through `Send` (single-flight + full-60s-429 backoff). The spectator poll also respects `LichessApi.Busy`.
    - **Token-hygiene audit (clean):** grep of logs/`[Sync]`/`[Rpc]` finds no token exposure; new code only ever passes the token into `LichessApi` (Authorization header). The one grep hit is `CancellationToken`, unrelated.
    - **Settings:** reviewed and kept as-is — every row (room theme/lights, table lights, checkerboard floor toggle, **pop frequency** (still drives the glyph re-pick), my/other board sounds) remains meaningful; nothing to trim.
    - **Deferred (still user-machine / optional):** Poly Haven 3D piece import + swap (D5 drop-in `models/chess/{type}.vmdl`), new thumbnail/branding.
  **M6 gate (user, in-editor):** floor pops render as opposite-colour piece glyphs on both square colours (iterate the shader if not); captures pop and moves tick/tock in lichess games + puzzles; full playtest.

## M1 scene rewire (one-time editor pass)

Component renames/deletions can't be applied to `lobby.scene` from this host (the
editor owns the format); do this once in the s&box editor, then save the scene:

1. **Room** GO: remove the missing `ArcadeRing`, `LeaderboardWall`, and
   `SpectatorBoard` components. **`ChessRing` adds itself** — LobbyRoom creates
   it on this GO if absent (self-heal for the rename); just save the scene so it
   persists. Defaults match the old tuning (Radius 180, 8 stations); retune in
   the inspector to taste.
2. **GameController** GO: remove the missing `GameController` and
   `MultiplayerController` components. Keep `SkafinityPlayer` (consider renaming
   the GO to "Music").
3. **UI** GO: remove the missing `SplashScreen`, `ModePickerScreen`,
   `MultiplayerScreen`, `GameHud`, and `LeaderboardScreen` components. Keep
   `ScreenPanel`, `LobbyOverlay`, `SettingsScreen`, `InfoScreen`, `ChatPanel`,
   `SkafinityMusicPanel`, `MusicBoardScreen`.
4. **Network** GO (`LobbyNetworkManager`): raise `MaxPlayers` from 8 to 16 — two
   players per table now.
5. **PlayerTemplate** (`LobbyPlayer`): scene overrides `InteractRange: 90`; the
   new seat-proximity default is 55 — lower it to ~55–60 so the "Press E" prompt
   doesn't reach across the table to the far seat.

Nothing else changed shape: walls, floor, music, chat, and the player template are
untouched, and station geometry (tables, signs, number panels) is runtime-built.

## Risks / open questions

1. **NDJSON streaming** — project-critical for *live moves-on-the-board* Board API play; no lichess polling alternative. **Corrected finding (2026-07-13, source-confirmed against `sbox-public`):**
   - **`Http.*` genuinely cannot stream.** `Http.RequestAsync` ends in `Client.SendAsync(request, ct)` with **no `HttpCompletionOption.ResponseHeadersRead`** → it defaults to `ResponseContentRead`, which buffers the whole body before returning. That is exactly why the M3 spike hung on the infinite TV feed — it never got *past* `RequestAsync` to its `ReadAsStreamAsync` line. Every `Http.*` helper (incl. `RequestStreamAsync`, which awaits `RequestAsync` first then hands back a MemoryStream over already-buffered bytes) funnels through this. `gambit_lichess_tv` reproduces it.
   - **Raw sockets are off-whitelist.** `System.Net.Http.HttpClient` itself, `System.Net.Sockets`, and `SslStream` are **not** in the access whitelist (`Sandbox.Access/Rules/BaseAccess.cs` allows only the message/content/header/status types) → SB1000. So we can't construct our own client with `ResponseHeadersRead`, nor hand-roll a TLS socket to lichess.
   - **What the M3 spike missed: `Sandbox.WebSocket`.** There *is* a whitelisted, gameplay-usable `WebSocket` (`engine/.../Utility/WebSocket.cs`, documented) that streams incrementally (`OnMessageReceived`) and takes custom headers. The spike only ever tested `Http.RequestAsync`, so "streaming is impossible" was overstated. **Catch:** lichess's Board API is HTTPS+NDJSON with no public `wss://`, so `WebSocket` can't hit lichess directly — it needs a **relay** (a small server, e.g. on gamah.net, that opens the lichess ndjson stream server-side and forwards each line over `wss://`; writes stay ordinary short `Http` POSTs). **User decision (2026-07-13): no hosted relay for now** — so live moves-on-the-board play stays deferred and M4 ships the **open-challenge link flow** (M4a above) instead: real sbox↔browser games with zero streaming and zero infra. If a relay is ever accepted, it's the one viable route to streaming; a Facepunch feature request for a headers-first `Http` read would remove the relay need entirely.
2. **Loopback/browser for PKCE** — token-paste fallback is guaranteed shippable.
3. **Chess library vs s&box whitelist** — compact hand-written move-gen budgeted as fallback (verified with perft).
4. **No toolchain on this host** — all verification is review + user-run gates; keep changes pattern-matched to proven code (AddBox, Rpc idioms).
5. **Shader glyph pops (D6)** need in-editor iteration — scheduled last (M6) so it never blocks gameplay.
6. **Scene rewire** — component renames require one manual in-editor pass (M1, documented).
7. **Two-seat station** breaks the old single-occupant invariant — touches enter/leave, camera, disconnect-cleanup, HUD routing; the main structural rework of the fork.
8. **Clock drift** — clocks interpolate between stream updates; resync from `gameFull` on reconnect.
9. **Attract/demo mode removed** (was Go-replay-based); "lichess TV on station 0" is unscoped stretch.

## Verification

- This session: after push, `gh repo view Gamah/gambit` confirms private repo; user clones/opens in s&box editor and confirms compile (M0 gate).
- Ongoing: each milestone gate above is the end-to-end test, run by the user in-editor/in-game; API-facing code can additionally be exercised on this host with a standalone `dotnet` harness (plain HttpClient against lichess) to validate endpoint handling before porting into s&box idioms.
